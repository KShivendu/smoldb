# Trigger directly with rusty-hook run --hook <pre-commit|pre-push>

[hooks]
pre-commit = "./tools/stash-hook.sh 'cargo +nightly fmt --all -- --check'"
pre-push = "./tools/stash-hook.sh 'cargo clippy --all-targets --all-features -- -D warnings'"

[logging]
verbose = true
